services:

  test:
    build:
      context: .
      dockerfile: Dockerfile.test
    container_name: fastapi_tests
    environment:
      DATABASE_URL: sqlite:///./test.db
    volumes:
      - .:/app
    restart: "no"
